# Delta — browser-interface

> The change expressed against the current spec as explicit operations.

## MODIFIED

- **AC-47**: a wrapped row is as tall as its own text needs, and the table places rows from
  measured heights rather than from one height times a count. The clamp to a fixed number of lines,
  and the tooltip that was where the rest of the text lived, are gone.

## ADDED

Nothing.

## REMOVED

Nothing.
